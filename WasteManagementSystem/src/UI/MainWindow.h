// MainWindow.h
// This file defines the main window UI component
#pragma once

/**
 * @brief Main application window
 */
class MainWindow {
public:
    MainWindow() = default;
    ~MainWindow() = default;

    /**
     * @brief Initialize the main window
     * @return True if initialization was successful
     */
    bool Initialize() { return true; }

    /**
     * @brief Render the main window content
     */
    void Render() {}
};